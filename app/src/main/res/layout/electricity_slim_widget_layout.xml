<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background_matcha_primary"
    android:orientation="horizontal"
    android:padding="4dp">

    <!-- 电量数字 -->
    <TextView
        android:id="@+id/tv_electricity_balance"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:gravity="center"
        android:text="--"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- 分隔线 (使用ImageView代替View) -->
    <ImageView
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:layout_marginVertical="4dp"
        android:background="@color/matcha_light"
        android:contentDescription="分隔线"/>

    <!-- 刷新按钮 -->
    <ImageButton
        android:id="@+id/btn_refresh"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        android:contentDescription="刷新电量"
        android:padding="4dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_refresh"
        android:tint="@color/white" />

</LinearLayout> 